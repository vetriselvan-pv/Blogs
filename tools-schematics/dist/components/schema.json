{
    "$schema": "http://json-schema.org/schema",
    "title": "Component",
    "description": "Creates a new predefined template component.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the component.",
            "x-prompt": "What name would you like to use for the component?"
        },
        "path": {
            "type": "string",
            "description": "The path to create the component in.",
            "default": "src/app"
        },
        "style": {
            "type": "string",
            "description": "The style format to use for the component.",
            "default": "scss",
            "enum": [
                "css",
                "scss",
                "sass",
                "less"
            ]
        },
        "skipTests": {
            "type": "boolean",
            "description": "Skip creating a test file for the component.",
            "default": false
        }
    },
    "required": [
        "name"
    ]
}